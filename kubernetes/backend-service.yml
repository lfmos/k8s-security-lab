apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cluster-sentinel
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cluster-sentinel
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
